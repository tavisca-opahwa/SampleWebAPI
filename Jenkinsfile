pipeline{
    agent {label 'master'}
    stages{
        stage('build'){
            steps{ 
                sh 'git clone https://github.com/tavisca-opahwa/SampleWebAPI.git'
            }
            
        }
    }
}